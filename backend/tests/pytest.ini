[pytest]
testpaths = .
python_files = test_phase4_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    integration: marks tests as integration tests requiring running services
addopts =
    -v
    --tb=short
    -m integration
